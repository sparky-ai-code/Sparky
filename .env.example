# Sparky is designed to run locally.
# Configure model-provider credentials from Settings > Models in the app.
#
# Do not commit API keys, signing credentials, production service URLs, or other secrets.
