# Plan Mode announcement provenance

- Canvas: 1600 x 900 pixels, suitable for an X image post.
- Background and Sparky sphere: preserved from the user-uploaded old announcement asset.
- Plan Mode icon: exact `PencilRulerIcon` geometry and stroke attributes used by `ChatComposer.tsx` when `interactionMode === "plan"`.
- Copy: human-authored announcement copy in `post.txt`.
- No generative image content used.
