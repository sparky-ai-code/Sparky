Create a sidebar like this for the UI make sure it appears exactly like it and shows real data no mock data, It must be on the left not on the right as a small pop-up that gets bigger with new assets getting into it Make it so it shows the dev servers that are running on the current directory and Make them appear as they are a browser tab so when i click on them it opens on the true sidebar browser. After implementing that do checks, varifactions and real visual tests using @browser plugin. When you finish checks & tests Rebuild all 3 installers via GitHub actions, We Need Windows exe, MacOS arm64 and MacOS X64, Linux remains coming soon, Read the website directory and edit the website to make install the latest installers via a GitHub Link then use Wrangler to redeploy the website under the Sparky.llc domain and its www.sparky.llc variant.