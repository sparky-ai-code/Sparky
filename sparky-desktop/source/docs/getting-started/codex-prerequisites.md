# Codex prerequisites

- Install Codex CLI so `codex` is on your PATH.
- Authenticate Codex before running Sparky (for example via API key or ChatGPT auth supported by Codex).
- Sparky starts the server via `codex app-server` per session.
